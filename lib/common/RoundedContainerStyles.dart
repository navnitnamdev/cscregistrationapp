import 'package:flutter/material.dart';

class RoundedContainerStyles {
  static const double defaultBorderRadius = 10.0;
  static const double defaultHeight = 50.0;
  static const double defaultWidth = 100.0;

  static const Color defaultColor = Colors.white;

  static const EdgeInsetsGeometry defaultPadding = EdgeInsets.all(10.0);
  static const EdgeInsetsGeometry defaultMargin = EdgeInsets.all(5.0);

  static const BoxShadow defaultBoxShadow = BoxShadow(
    color: Colors.grey,
    blurRadius: 10.0,
    offset: Offset(0, 5),
  );
}
